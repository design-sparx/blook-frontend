import React from 'react';
import EmptyState from './empty-state';

export default {
  title: 'Sharing post options',
  component: EmptyState
};

export const Default = (): JSX.Element => <EmptyState />;
