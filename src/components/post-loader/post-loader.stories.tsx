import React from 'react';
import PostLoader from './post-loader';

export default {
  title: 'Sharing post options',
  component: PostLoader
};

export const Default = (): JSX.Element => <PostLoader size='md'/>;
